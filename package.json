{
  "name": "lost_jump",
  "version": "1.0.0",
  "scripts": {
    "dev:web": "vite",
    "build:web": "vite build",
    "preview:web": "vite preview",
    "dev": "npm run build:web && electron .",
    "dist": "npm run build:web && electron-builder",
    "android": "npm run build:web && npx cap copy && npx cap open android",
    "android:build": "npm run build:web && npx cap copy"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3",
    "rollup-plugin-copy": "^3.5.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/cli": "^7.5.0",
    "@capacitor/core": "^8.1.0",
    "gl-matrix": "^3.4.4"
  }
}
